<a href="/">Back to all blocks</a>
<h2>{{title}}</h2>
<p>
  {{#if prevLink}}
    <a href="{{prevLink}}">&lt Block {{prevHeight}}</a>
  {{/if}}
  {{#if nextLink}}
    <a href="{{nextLink}}">Block {{nextHeight}} &gt;</a>
  {{/if}}
</p>
<br />
<h3>Header</h3>
<table>
  <tr>
    <th>Hash</th>
    <td>{{hex this.header.hash}}</td>
  </tr>
  <tr>
    <th>Proof of Work</th>
    <td>{{lookup this.pows this.header.pow.pow_algo}}</td>
  </tr>
  <tr>
    <th>Height</th>
    <td>{{this.header.height}}</td>
  </tr>
  <tr>
    <th>Version</th>
    <td>{{this.header.version}}</td>
  </tr>
  <tr>
    <th>Timestamp</th>
    <td>{{timestamp this.header.timestamp}}</td>
  </tr>
  <tr>
    <th>Previous Hash</th>
    <td>{{hex this.header.prev_hash}}</td>
  </tr>
  <tr>
    <th>Nonce</th>
    <td>{{this.header.nonce}}</td>
  </tr>
  <tr>
    <th>Output Merkle Root</th>
    <td>{{hex this.header.output_mr}}</td>
  </tr>
  <tr>
    <th>Witness Merkle Root</th>
    <td>{{hex this.header.witness_mr}}</td>
  </tr>
  <tr>
    <th>Kernel Merkle Root</th>
    <td>{{hex this.header.kernel_mr}}</td>
  </tr>
  <tr>
    <th>Input Merkle Root</th>
    <td>{{hex this.header.input_mr}}</td>
  </tr>
  <tr>
    <th>Kernel MMR Size</th>
    <td>{{this.header.kernel_mmr_size}}</td>
  </tr>
  <tr>
    <th>Output MMR Size</th>
    <td>{{this.header.output_mmr_size}}</td>
  </tr>
  <tr>
    <th>Total Kernel Offset</th>
    <td>{{hex this.header.total_kernel_offset}}</td>
  </tr>
  <tr>
    <th>Total Script Offset</th>
    <td>{{hex this.header.total_script_offset}}</td>
  </tr>
</table>

{{#with this.body}}
  <table>
    <thead>
      <tr>
        <th>
          <h2>Outputs({{outputs_length}})</h2>
          <p>
            {{#if outputsPrevLink}}
              <a href="{{outputsPrevLink}}">&lt Outputs {{outputsPrev}}</a>
            {{/if}}
            {{#if outputsNextLink}}
              <a href="{{outputsNextLink}}">Outputs {{outputsNext}} &gt;</a>
            {{/if}}
          </p>
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each outputs as |output|}}
        <tr>
          <td>
            <h3>Output {{add @index ../outputsFrom}}</h3>
            {{>TransactionOutput output}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <table>
    <thead>
      <tr>
        <th>
          <h2>Inputs({{inputs_length}})</h2>
          <p>
            {{#if inputsPrevLink}}
              <a href="{{inputsPrevLink}}">&lt Inputs {{inputsPrev}}</a>
            {{/if}}
            {{#if inputsNextLink}}
              <a href="{{inputsNextLink}}">Inputs {{inputsNext}} &gt;</a>
            {{/if}}
          </p>
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each inputs as |input|}}
        <tr>
          <td>
            <h3>Input {{add @index ../inputsFrom}}</h4>
            {{>TransactionInput input}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  <table>
    <thead>
      <tr>
        <th>
          <h2>Kernels({{kernels_length}})</h2>
          <p>
            {{#if kernelsPrevLink}}
              <a href="{{kernelsPrevLink}}">&lt Kernels {{kernelsPrev}}</a>
            {{/if}}
            {{#if kernelsNextLink}}
              <a href="{{kernelsNextLink}}">Kernels {{kernelsNext}} &gt;</a>
            {{/if}}
          </p>
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each kernels as |kernel|}}
        <tr>
          <td>
            <h3>Kernel {{add @index ../kernelsFrom}}</h4>
            {{>TransactionKernel kernel}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{/with}}
